# 2026-08-28 Vercel 部署與存取保護查核

| 項目 | 結果 |
|---|---|
| GitHub `main` 對應 commit | `c16e51858c62d1b76960ec7f2a4959d7f0d29072` |
| Vercel production deployment | `READY` |
| HTML 可用性 | 使用 Vercel 授權讀取介面取得 `HTTP 200 OK` 與繁體中文 SPA 入口 |
| 一般瀏覽器直接開啟 | 被導向 Vercel 登入頁，表示 deployment protection 目前生效 |
| 有效存取保護 | Vercel SSO 已啟用，範圍為「所有非自訂網域」；密碼保護與 Trusted IP 未啟用 |

此查核證實 GitHub `main` 已觸發成功的 production deployment，但目前無法以未登入的一般瀏覽器直接進行新版 UI 的正式站視覺驗證。由於 SSO 規則不套用至自訂網域，若面向學生公開使用，應使用已綁定的自訂網域；若尚未綁定自訂網域，專案擁有者需在 Vercel 專案的 Deployment Protection 設定調整公開存取範圍，並在調整前審視安全影響。

## 使用者確認後的公開存取驗證

專案擁有者已確認允許公開 production 存取，因此關閉 Vercel SSO protection；密碼保護與 Trusted IP 均維持未啟用。隨後以未登入瀏覽器直接開啟最新 production deployment，成功取得繁體中文首頁與可見操作入口：年級／單元選擇、三種核准解題模式、單元化建議題目、公式編輯器，以及「可一次匯入最多 5 個照片、PDF、TXT 或 Markdown 題目」的多題匯入按鈕。

登入後的教師教材工作台、常犯錯題、學習紀錄與匯出內容屬於個人／角色保護資料，仍應使用受控測試帳號完成端對端驗證；未登入公開檢查不會也不應顯示這些資料。
